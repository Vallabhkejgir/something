# app/guardrails/__init__.py
"""
Guardrails package — Input and output safety for the RAG pipeline.

Modules:
  input_guard    — Validates incoming queries
  output_guard   — Validates and sanitises generated answers
  prompt_armor   — Defense-in-depth prompt injection utilities
"""
